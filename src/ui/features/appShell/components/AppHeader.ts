/** Presentation: the app title + subtitle block. */
export function appHeader(): string {
  return `  <div class="app-header">
    <h1>Code Change Understanding Agent</h1>
    <p>Upload a change, add the requirement, and turn the analysis into PR notes, flows, and team updates.</p>
  </div>`;
}
