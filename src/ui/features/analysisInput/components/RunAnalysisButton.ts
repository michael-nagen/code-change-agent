/** Presentation: the single primary "Run Initial Analysis" button. */
export function runAnalysisButton(): string {
  return `    <div style="margin-top:16px">
      <button type="button" id="run-btn" class="primary-btn">Run Initial Analysis</button>
    </div>`;
}
