node_modules/
dist/

# Local environment files — never commit real secrets.
# `.env.example` is intentionally tracked (see negation below).
.env
.env.local
!.env.example
.vercel

# Persistent developer memory (local file store). Never commit real memory data.
data/
