# Secrets — configure these as Environment Variables in the Vercel project.
.env
.env.local

# Agent workspace files that are irrelevant to the deployed app.
# Anchor with a leading slash so these only match the repo root and do NOT
# accidentally strip source dirs like src/memory/.
/memory/
/builder-skill/
/docs/
.claude/
.cursor/
*.md
!README.md
